# Prometheus scrape configuration for llmrouter local development.
# Mounted into the Prometheus container by docker-compose.yaml.

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "llmrouter"
    static_configs:
      - targets: ["host.docker.internal:8080"]
